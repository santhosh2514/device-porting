// Import Models Always on Aschending Order, It will be easy to quickly check whether the model imported or not.
const User = require("./User");
const Conversation = require("./Conversation");

module.exports = {
    User,
    Conversation
}
